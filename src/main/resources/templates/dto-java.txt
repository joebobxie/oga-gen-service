package [(${config.packageName})].module.[(${table.config.catalog})].dto;

import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.Accessors;
import [(${config.packageName})].model.dto.BaseDTO;
[# th:each="item,stat: ${table.dbType}"]
[(${item})]
[/]

/**
* [(${table.describe})]
*/
@Data
@Accessors(chain = true)
@EqualsAndHashCode(callSuper = false)
@ApiModel(value="[(${table.describe})]", description="[(${table.describe})]")
public class [(${table.className})]DTO extends BaseDTO {

[# th:each="item,stat: ${table.children}"]
    /**
     * [(${item.describe})]
     */
    @ApiModelProperty(value = "[(${item.describe})]", required = true)
    private [(${item.fieldType})] [(${item.javaName})];

[/]
}
